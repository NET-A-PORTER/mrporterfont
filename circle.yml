dependencies:
  pre:
    - ls -l
    - rm -rf node_modules
    - npm install
    - npm install -g gulp

test:
  override:
    - echo 'add test command'

deployment:
  production:
    branch: master
    commands:
      - ls -l
      - echo "doing a patch update!"
      - git config --global user.email "mrportertechteam@mrporter.com"
      - git config --global user.name "mrporterdev"
      - npm version patch
      - gulp font
      - gulp tag
      - git commit -a -m 'automated patching [ci skip]'
      - git push
